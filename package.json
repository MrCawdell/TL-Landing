{
  "name": "tl-landing-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright checks for the TipperLink marketing site",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1"
  }
}
